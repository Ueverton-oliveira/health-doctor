<h3>Lista de Médicos</h3>
<table class="table">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Nome Completo</th>
      <th scope="col">E-mail</th>
      <th scope="col">CRM</th>
      <th scope="col">CRM/UF</th>
      <th scope="col">
        <%= link_to new_doctors_backoffice_doctor_path, class:"btn btn-primary btn-success btn-sm" do %>
          <i class="fas fa-plus"></i>
        <% end %>
      </th>
    </tr>
  </thead>
  <tbody>
    <% @doctors.each do |doctor| %>
    <tr>
      <th scope="row"><%= doctor.id %></th>
      <td><%= doctor.name %></td>
      <td><%= doctor.email %></td>
      <td><%= doctor.crm %></td>
      <td><%= doctor.crm_uf %></td>
      <td>
        <%= link_to edit_doctors_backoffice_doctor_path(doctor), class:"btn btn-primary btn-circle btn-sm" do %>
          <i class="fas fa-edit"></i>
        <% end %>
        <%= link_to doctors_backoffice_doctor_path(doctor), method: :delete, class:"btn btn-danger btn-circle btn-sm", data: { confirm: "Confirmar exclusão!"} do %>
          <i class="fas fa-trash"></i>
        <% end %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
<%= paginate @doctors %>
